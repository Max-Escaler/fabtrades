# Copy to .env and fill in. NEVER commit the real .env.
# FABTrades pipeline: ingests Flesh and Blood (FAB) card + price data into the shared Supabase project.
# Service role key: Supabase Dashboard -> Project Settings -> API -> service_role (secret).
# It bypasses RLS and must only be used server-side / in CI, never in a client app.
SUPABASE_URL=https://tenrvaghaspwdvnwvgrh.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here
